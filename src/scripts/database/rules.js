
import values from "./values.js";
// let valkeys = Object.keys(values);
// let valvalues = Object.values(values);

const rules = () => {
    for (const c in values) {
        console.log(values)
        break
    }
}

export default rules;

// return {
//     comprimento: [
//         "km > hm > dam > m > dm > cm > mm",
//         "km < hm < dam < m < dm < cm < mm"
//     ],
// }